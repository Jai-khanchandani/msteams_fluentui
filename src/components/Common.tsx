import { Input, Dropdown } from "@fluentui/react-northstar"


export const TextField = (props: any) => {
    return <Input inverted {...props}></Input>
}

export const DropDown = (props: any) => {
    return <Dropdown inverted {...props}></Dropdown>
}